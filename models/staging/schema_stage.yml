
version: 2

models:
  - name: coordinate_stage
    description: "Coordinate table in stage"
    columns:
      - name: coordinate_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: zone_stage
    description: "Zone table in stage"
    columns:
      - name: location_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
